step 1: first do sort given array
step 2: swap alternate
for swap alternate you have to swap [i] and [i+1]
you have travers complete loop 
increment is by +2
code:
class Solution {
public:
    vector<int> numberGame(vector<int>& nums) {
        sort(nums.begin() , nums.end());
        for(int i=0; i<nums.size(); i=i+2){
           
                swap(nums[i] , nums[i+1]);
            
        }
      return nums;  
    }
};
