Input: n = 5
Output: 7
Explanation:
The binary representation of 7 is "111".
#simply make power of 2 into x when x is bigger than n break and return x-1
#code:
int smallestNumber(int n) {
    int x = 1;
    while (x <= n) x *= 2;
    return x - 1;
}
